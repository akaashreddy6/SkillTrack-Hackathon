import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { PageHeader, PlatformLayout } from "../components/Platform";
import { deleteProject, getProjects, getSkills, saveProject } from "../services/skilltrackService";
import { useAuth } from "../context/AuthContext";

const emptyProject = { title: "", description: "", technologies: "", github_url: "", live_demo_url: "", image_url: "", featured: false };

export default function Portfolio() {
  const { user, profile } = useAuth();
  const [projects, setProjects] = useState([]); const [skills, setSkills] = useState([]); const [selectedSkills, setSelectedSkills] = useState([]); const [project, setProject] = useState(emptyProject); const [state, setState] = useState({ loading: true, saving: false, error: "", message: "" });
  const load = async () => { try { const [items, catalog] = await Promise.all([getProjects(user.id), getSkills()]); setProjects(items); setSkills(catalog); } catch (error) { setState((previous) => ({ ...previous, error: error.message || "Unable to load your portfolio." })); } finally { setState((previous) => ({ ...previous, loading: false })); } };
  useEffect(() => { load(); }, [user.id]);
  const submit = async (event) => { event.preventDefault(); setState((previous) => ({ ...previous, saving: true, error: "", message: "" })); try { await saveProject({ userId: user.id, project, skillIds: selectedSkills }); setProject(emptyProject); setSelectedSkills([]); setState((previous) => ({ ...previous, saving: false, message: "Project saved." })); await load(); } catch (error) { setState((previous) => ({ ...previous, saving: false, error: error.message || "Unable to save project." })); } };
  const edit = (item) => { setProject({ id: item.id, title: item.title, description: item.description || "", technologies: item.technologies || "", github_url: item.github_url || "", live_demo_url: item.live_demo_url || "", image_url: item.image_url || "", featured: item.featured }); setSelectedSkills(item.project_skills?.map((skill) => skill.skill_id) || []); window.scrollTo({ top: 0, behavior: "smooth" }); };
  const remove = async (id) => { try { await deleteProject(id, user.id); setProjects((items) => items.filter((item) => item.id !== id)); } catch (error) { setState((previous) => ({ ...previous, error: error.message || "Unable to delete project." })); } };
  return <PlatformLayout><PageHeader eyebrow="PORTFOLIO" title={`${profile?.full_name || "Your"} portfolio`} description="Show the work behind your skills and give employers more evidence of your capability." action={<Link className="button button-secondary" to="/profile">Edit profile</Link>} />{state.error && <div className="data-error">{state.error}</div>}{state.message && <div className="auth-success data-feedback">{state.message}</div>}<form className="panel project-form" onSubmit={submit}><div className="panel-header"><div><p className="eyebrow">PROJECTS</p><h2>{project.id ? "Edit project" : "Add a project"}</h2></div><span>Build your evidence library</span></div><div className="form-grid"><label>Project title<input required value={project.title} onChange={(event) => setProject({ ...project, title: event.target.value })} /></label><label>Technologies<input placeholder="React, Supabase, CSS" value={project.technologies} onChange={(event) => setProject({ ...project, technologies: event.target.value })} /></label><label className="form-wide">Description<textarea required value={project.description} onChange={(event) => setProject({ ...project, description: event.target.value })} /></label><label>GitHub URL<input type="url" placeholder="https://github.com/..." value={project.github_url} onChange={(event) => setProject({ ...project, github_url: event.target.value })} /></label><label>Live demo URL<input type="url" placeholder="https://..." value={project.live_demo_url} onChange={(event) => setProject({ ...project, live_demo_url: event.target.value })} /></label><label>Project image URL<input type="url" placeholder="https://..." value={project.image_url} onChange={(event) => setProject({ ...project, image_url: event.target.value })} /></label></div><fieldset><legend>Skills demonstrated</legend><div className="skill-checkboxes">{skills.map((skill) => <label key={skill.id}><input type="checkbox" checked={selectedSkills.includes(skill.id)} onChange={() => setSelectedSkills((current) => current.includes(skill.id) ? current.filter((id) => id !== skill.id) : [...current, skill.id])} />{skill.name}</label>)}</div></fieldset><label className="checkbox-row"><input type="checkbox" checked={project.featured} onChange={(event) => setProject({ ...project, featured: event.target.checked })} />Feature this project</label><button className="button button-primary" disabled={state.saving}>{state.saving ? "Saving..." : project.id ? "Update project" : "Save project"}</button>{project.id && <button type="button" className="button button-secondary" onClick={() => { setProject(emptyProject); setSelectedSkills([]); }}>Cancel edit</button>}</form><section className="portfolio-grid">{state.loading && <div className="route-state">Loading your projects...</div>}{!state.loading && !projects.length && <div className="empty-state">No projects yet. Add your first project to strengthen your portfolio.</div>}{projects.map((item) => <article className="project-card panel" key={item.id}>{item.image_url && <img src={item.image_url} alt="" />}{item.featured && <span className="level-badge">Featured</span>}<h2>{item.title}</h2><p>{item.description}</p>{item.technologies && <div className="job-skills">{item.technologies.split(",").map((technology) => <span key={technology}>{technology.trim()}</span>)}</div>}<div className="job-skills">{item.project_skills?.map((skill) => <span key={skill.skill_id}>{skill.skills?.name}</span>)}</div><div className="project-links">{item.github_url && <a href={item.github_url} target="_blank" rel="noreferrer">GitHub</a>}{item.live_demo_url && <a href={item.live_demo_url} target="_blank" rel="noreferrer">Live demo</a>}</div><div className="project-actions"><button className="table-action" onClick={() => edit(item)}>Edit</button><button className="table-action danger-action" onClick={() => remove(item.id)}>Delete</button></div></article>)}</section></PlatformLayout>;
}
